import { TituloHtmlPipe } from './titulo-html.pipe';

describe('TituloHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new TituloHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
